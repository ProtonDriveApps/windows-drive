<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Proton.Drive.App.Windows.Controls"
    xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options">

    <SolidColorBrush
        x:Key="SplitButton.Separator.Brush"
        po:Freeze="True"
        Color="Gray" />

    <Style
        x:Key="ToggleSplitButtonStyle"
        TargetType="{x:Type controls:ToggleSplitButton}">
        <Setter Property="Background" Value="{StaticResource Button.Default.Background}" />
        <Setter Property="Foreground" Value="{StaticResource Text.DarkTheme.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Button.FontSize}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="18,6" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource Button.DarkTheme.FocusVisualStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ToggleSplitButton}">
                    <Grid>
                        <Border
                            x:Name="OuterBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource Button.CornerRadius}"
                            SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border
                                    x:Name="PrimaryBackground"
                                    Grid.Column="0"
                                    Background="Transparent"
                                    CornerRadius="4,0,0,4"
                                    SnapsToDevicePixels="True">
                                    <ContentPresenter
                                        x:Name="PrimaryContent"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        TextElement.FontSize="{TemplateBinding FontSize}"
                                        TextElement.Foreground="{TemplateBinding Foreground}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        RecognizesAccessKey="True"
                                        Margin="{TemplateBinding Padding}"
                                        Focusable="False" />
                                </Border>

                                <Grid
                                    x:Name="SecondaryBackground"
                                    Grid.Column="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Rectangle
                                        Grid.Column="0"
                                        Fill="{StaticResource SplitButton.Separator.Brush}"
                                        Opacity="0.5"
                                        Width="1"
                                        VerticalAlignment="Stretch"
                                        Margin="0,6" />

                                    <Button
                                        x:Name="PART_DropDownButton"
                                        AutomationProperties.AutomationId="PauseOptionsDropDownButton"
                                        Grid.Column="1"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Focusable="False">
                                        <Button.Template>
                                            <ControlTemplate TargetType="{x:Type Button}">
                                                <Border
                                                    x:Name="ChevronBackground"
                                                    Background="{TemplateBinding Background}"
                                                    CornerRadius="0,4,4,0"
                                                    SnapsToDevicePixels="True"
                                                    Padding="10,0">
                                                    <TextBlock
                                                        x:Name="Chevron"
                                                        Text="{StaticResource icon-chevron-down}"
                                                        FontSize="10"
                                                        Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type controls:ToggleSplitButton}}}"
                                                        RenderTransformOrigin="0.5,0.5"
                                                        Style="{StaticResource FontIconStyle}"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center" />
                                                </Border>

                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ChevronBackground" Property="Background" Value="{StaticResource Button.MouseOver.Background}" />
                                                    </Trigger>

                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="Chevron" Property="Opacity" Value="0.4" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger SourceName="PrimaryBackground" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PrimaryBackground" Property="Background" Value="{StaticResource Button.MouseOver.Background}" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource Button.Disabled.Background}" />
                            <Setter Property="Foreground" Value="{StaticResource GrayBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        TargetType="{x:Type controls:ToggleSplitButton}"
        BasedOn="{StaticResource ToggleSplitButtonStyle}" />
</ResourceDictionary>
